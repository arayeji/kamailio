# HTTP NMS API on P-CSCF (native ims_nms_api module via xhttp)
# Enable with #!define WITH_NMS_API

#!ifdef WITH_NMS_API

include_file "../nms-api.cfg"

loadmodule "xhttp.so"
loadmodule "ims_nms_api.so"

event_route[xhttp:request] {
	ims_nms_api_dispatch();
}

#!endif
